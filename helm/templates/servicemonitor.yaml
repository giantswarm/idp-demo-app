{{- if .Values.monitoring.serviceMonitor.enabled | default false -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "laszlo-kratix-6.fullname" . }}
  labels:
    {{- include "laszlo-kratix-6.labels" . | nindent 4 }}
spec:
  endpoints:
  - path: /metrics
    port: http
  selector:
    matchLabels:
      {{- include "laszlo-kratix-6.selectorLabels" . | nindent 4 }}
{{- end }}
