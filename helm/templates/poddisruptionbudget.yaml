{{- if .Values.pdb.enabled | default false -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "laszlo-kratix-6.fullname" . }}
  labels:
    {{- include "laszlo-kratix-6.labels" . | nindent 4 }}
spec:
  minAvailable: 2
  selector:
    matchLabels:
      {{- include "laszlo-kratix-6.selectorLabels" . | nindent 6 }}
{{- end }}
